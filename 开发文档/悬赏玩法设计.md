# 悬赏玩法设计文档

## 一、功能概述

在议事厅新增"悬赏任务"入口，玩家可通过 LLM 生成一批悬赏通缉令，接取后在大地图对应地点触发定向战斗，根据战斗结果获得声望和赏金奖励。

**仅影响独立前端链路（index.html），不影响 index - SR.html 的 SillyTavern 链路。**

---

## 二、完整事件流（两轮 LLM 交互）

```
[议事厅] 点击"悬赏任务"按钮
    ↓
显示悬赏弹窗
  ├─ 无任务时：弹窗内容为空，底部显示"发布悬赏"按钮
  └─ 有进行中任务时：高亮显示当前接取的任务卡，底部显示"放弃任务"按钮
    ↓
点击"发布悬赏"
    ↓
调用 generateBountyTasks()
    → LLM API 请求（参考 summary-runner.js 的调用方式）
    → 解析 JSON 响应，得到 3 个悬赏任务
    → 以任务卡形式渲染到弹窗
    ↓
玩家点击某张任务卡（接取任务）
    → acceptBounty(task) 将任务写入 activeBounty（同步到 gameData）
    → 弹窗关闭
    ↓
玩家前往 [山门] → 点击"下山"
    → showWorldMap() 附带 bountyLocation 参数传给大地图 iframe
    ↓
[world_map.html]
    → 识别 bountyLocation 参数
    → 在对应地点图标上叠加"⚔"悬赏标记
    → 玩家选择该地点出发时，确认弹窗显示"⚠ 此地有悬赏目标！"
    → confirmTravel() 的 postMessage 中添加 bountyBattle: 1
    ↓
[game-events.js] worldmap-exit 消息处理
    → 检测到 bountyBattle === 1
    → 构造 user 输入：
        时间/季节/地点/随行NPC
        ${_actorName}行动选择：悬赏缉拿
        （不在 user message 中重复悬赏目标，行动指导 EJS 模板已通过 gameData.activeBounty 包含完整信息）
    → 设置 currentBattleType = 'bounty'（在发送前设置，供后续判断用）
    → 沿用现有 worldmap-exit 逻辑中的地点信息迭代记录：
        gameData.locationVisit = { active: true, location: mapLocation, startUiIndex: ..., startWeek: currentWeek }
        （与普通下山游历一致，不因走 bounty 分支而跳过，保证"地点信息迭代方案"功能不受影响）
    → 调用 handleMessageOutput(userMessage)
    ↓
════════════════════════════════
 第一轮 LLM 交互
════════════════════════════════
    → prompt-builder.js 识别 key "行动选择：悬赏缉拿"
    → worldbook-engine 自动注入 PROMPT_ACTION_BOUNTY 行动指导
      （指导 LLM：叙述下山追踪的过程，在 SIDENOTE 中构造悬赏战斗事件）
    → LLM 输出：
        MAIN_TEXT：下山→路途→抵达目的地→遭遇悬赏目标的剧情叙述
        SIDENOTE：包含 随机事件（事件类型：战斗事件），enemy 信息与 activeBounty 一致
    ↓
parseLLMResponse 解析第一轮响应
    → 处理 MAIN_TEXT（正常显示剧情）
    → 检测到 随机事件.事件类型 === '战斗事件'
    → 若 currentBattleType === 'bounty'：
        用 activeBounty 覆盖 currentBattleEvent（忽略 SIDENOTE 中的名称/武学/报酬，以变量为准）
    → displayBattleEvent(currentBattleEvent)
        （注：该函数只是设置 currentBattleEvent/currentBattleReward 并写入 DOM 文本，
         不会主动 `.show` 弹出；实际弹出显示由 updateStoryText() 统一控制——
         正文分页翻到最后一页时才会显示战斗事件框，悬赏分支无需额外处理，
         沿用现有 MAIN_TEXT 分页机制即可自然衔接）
    → 显示"开始战斗"按钮，等待玩家点击
    ↓
玩家点击"开始战斗"
    → 在按钮 handler 中保护 currentBattleType：
        if (currentBattleType !== 'bounty') currentBattleType = 'event'
        ← 只防止被意外覆盖，不重新推导（worldmap-exit 已赋值为准）
    → showBattleGame(battleData) 开启战斗 iframe
    ↓
战斗结束，[game-events.js] battle-exit 消息处理
    → currentBattleType === 'bounty' 分支
    ↓
【变量结算阶段（在发请求前完成，确保快照干净）】
    ① 同步道具数量（remainingItems）
    ② 胜利时：应用声望+赏金奖励，generateBattleEventDrop() 掉落计算
    ③ 清空 activeBounty = null
    ④ syncGameDataFromVariables()  ← 把①②③全部写回 gameData
    ⑤ showModal() 显示奖励弹窗（异步，不阻塞后续）
    ↓
【发起第二轮请求】
    ⑥ handleMessageOutput(battleResultMessage)
        → 内部自动 saveFullSnapshot()  ← 快照保存已结算、已清空任务的干净状态
        → 内部自动 saveAppState()
        → pipeline.runTurn()
    ↓
════════════════════════════════
 第二轮 LLM 交互
════════════════════════════════
    → LLM 正常输出战斗后的后续剧情
    ↓
parseLLMResponse 解析第二轮响应（普通流程）

【重生成安全性】
    若第二轮请求失败重生成，restoreFromSnapshot() 恢复的是步骤④之后的状态
    （注：项目中实际对应的恢复函数是 storageService.loadSnapshot()，不存在名为
     restoreFromSnapshot 的独立函数，这里仅指代"读取快照恢复状态"这一机制）
    奖励已结算、activeBounty 已清空，不会发生重复结算
```

---

## 三、数据结构

### 3.1 activeBounty（单条进行中任务）

```javascript
// game-state.js 新增变量
let activeBounty = null;

// 结构示例：
{
  enemyName: "刀疤虎",          // 悬赏对象名称
  locationName: "高昌",          // 所在地点（必须与 world_map.html 的地点 key 完全一致）
  level: 5,                    // 敌人等级 1-9（对应战斗 iframe 的 level 参数）
  description: "此人率众在高昌城西劫掠丝路商队，致数十人死伤，官府无力缉拿",
  reputationReward: 25,         // 声望奖励
  goldReward: 500              // 赏金（金钱奖励）
}
```

### 3.2 bountyList（本次生成的任务列表，不持久化）

```javascript
// bounty-service.js 内部变量，不存入 gameData
// 关闭弹窗即丢弃，点"发布悬赏"重新生成
let _currentBountyList = [];
```

---

## 四、LLM Prompt 设计

### 4.1 System Prompt

```
你是武侠世界的悬赏公告栏。请生成3张不同的悬赏通缉令。要求：
- 风格贴近五代十国末期西域武侠风格
- 人名/绰号有江湖气息
- 罪行描述简洁有画面感，不超过50字
- 地点必须从给定的列表中选择
- 难度分布合理，3个任务等级不完全相同
严格按 JSON 格式输出，不输出任何其他文字。
```

### 4.2 User Prompt（动态构造）

```
当前游戏背景：北宋初期，西域天山派。
玩家当前武学值：{playerStats.武学}（0-100）。
本周：第{year}年第{month}月第{week}周，季节：{season}。

可选地点列表：高昌、沙州、龟兹、伊州、哈密绿洲、月牙泉
（地点名称必须与 world_map.html 的地点 key 完全一致，如实际 key 为 '沙州' 而非 '沙洲城'）

请生成3张悬赏通缉令，JSON格式如下（直接输出JSON，不加任何markdown代码块）：
{"bounties":[
  {"enemyName":"...","locationName":"高昌","level":3,"description":"...","reputationReward":15,"goldReward":300},
  {"enemyName":"...","locationName":"沙州","level":5,"description":"...","reputationReward":25,"goldReward":500},
  {"enemyName":"...","locationName":"哈密绿洲","level":7,"description":"...","reputationReward":35,"goldReward":700}
]}

level建议参考玩家武学值：武学<20对应1-3级，武学20-50对应2-5级，武学50-80对应3-7级，武学>80对应5-9级。
reputationReward约为level×5，goldReward约为level×100。
```

### 4.3 响应解析

```javascript
// 提取 JSON 字符串（容错 markdown 代码块包裹的情况）
function parseBountyResponse(text) {
    const cleaned = text.replace(/```json|```/g, '').trim();
    const data = JSON.parse(cleaned);
    if (!Array.isArray(data.bounties) || data.bounties.length === 0) throw new Error('格式无效');
    return data.bounties;
}
```

---

## 五、改动范围

### 5.1 新增文件：`module/bounty-service.js`

**对外暴露的函数：**

| 函数 | 说明 |
|------|------|
| `showBountyModal()` | 打开悬赏弹窗（显示当前任务 or 空列表） |
| `generateBountyTasks()` | 调 LLM API 生成 3 个任务，渲染任务卡 |
| `acceptBounty(task)` | 接取任务，写入 activeBounty，关闭弹窗 |
| `abandonBounty()` | 放弃当前任务，清空 activeBounty |

**注意事项：**
- API 调用参考 `summary-runner.js` 的 `_sendForSummary()`：**不存在 `apiService.sendMessage()` 这个函数**，实际应按 `apiService.getConfig().streamMode` 动态选择：
  - 非流式：`apiService.sendMessages(messages, { signal, maxOutputTokens: null })`，返回 `{content, usage}`
  - 流式：`apiService.sendMessagesStream(messages, { onToken, onThinking, onComplete(fullText, usage), onError }, { maxOutputTokens: null })`
  - `messages` 为 `[{role:'system', content:...}, {role:'user', content:...}]` 数组，而非单条 message
- 需要加 loading 状态（点击"发布悬赏"后按钮变灰 + 显示"生成中..."）
- 如果 LLM 返回解析失败，弹 `showModal('生成失败，请重试')`

---

### 5.2 新增行动指导：`char_card_information/100行动选择-悬赏缉拿.txt` + `prompt-data-actions.js`

**`100行动选择-悬赏缉拿.txt` 内容（模板）：**

```
正文输出指导:
  基本要求:
    剧情连贯: 延续之前的剧情，流畅过渡到{{user}}接下悬赏任务、出山追踪目标的情境
    格式要求: 正文包裹在 <MAIN_TEXT></MAIN_TEXT> 内
    事件概述: {{user}}此行目的是追踪并缉拿悬赏目标，在目标所在地遭遇敌人进入战斗
    输出人称: 第二人称"你"
    参考元素:
      - 目的地: <%- gameData.mapLocation %>
      - 悬赏目标: <%- gameData.activeBounty?.enemyName %>（<%- gameData.activeBounty?.description %>）
      - 随行NPC: <%- gameData.companionNPC %>

  剧情特色:
    启程段落:
      - 接下悬赏令后的心理/准备动作（简短）
      - 随行者的反应/意见
    路途段落（简略带过）:
      - 打探目标行踪的过程
      - 路途上的小见闻或伏笔
    遭遇段落:
      - 在目的地发现目标，或目标主动现身
      - 目标登场的气氛渲染（外貌/气势/随从）
      - 双方交锋前的简短对话或对峙

  注意事项:
    - 不要在正文中描写战斗过程，战斗结果由游戏系统决定
    - 正文在"双方即将交手"的紧张时刻结束
    - 必须在 SIDENOTE 中构造战斗事件（格式见下方）

  SIDENOTE战斗事件格式（严格遵守，随机事件字段必须存在）:
    随机事件:
      事件类型: 战斗事件
      事件描述: 悬赏缉拿：<%- gameData.activeBounty?.enemyName %>
      敌方信息:
        名称: <%- gameData.activeBounty?.enemyName %>
        属性:
          武学: <%- gameData.activeBounty?.level %>
          生命力: 中
          攻击力: 中
        战斗报酬:
          类型: 金钱
          数值: <%- gameData.activeBounty?.goldReward %>
```

**`prompt-data-actions.js` 新增注册（在现有 keys 数组中追加）：**
```javascript
{ keys: ["行动选择：悬赏缉拿"], varName: 'PROMPT_ACTION_BOUNTY' }
```

---

### 5.3 修改 `module/game-state.js`

**新增变量：**
```javascript
let activeBounty = null;
```

**`syncGameDataFromVariables()` 新增：**
```javascript
gameData.activeBounty = activeBounty;
```

**`syncVariablesFromGameData()` 新增：**
```javascript
activeBounty = gameData.activeBounty || null;
```

---

### 5.4 修改 `index.html`

**（1）议事厅按钮新增：**
```html
<div class="scene-actions">
    <button class="scene-btn" onclick="performAction('汇报', 'yishiting')">汇报</button>
    <button class="scene-btn" onclick="showBountyModal()">悬赏任务</button>
</div>
```

**（2）悬赏弹窗 HTML（插入现有 modal 区域附近）：**
```html
<div id="bounty-modal" style="display:none; position:fixed; ...">
    <div class="bounty-modal-inner">
        <div class="bounty-modal-title">悬赏公告</div>
        <div id="bounty-list-container"><!-- 任务卡动态渲染 --></div>
        <div class="bounty-modal-footer">
            <button id="bounty-generate-btn" onclick="generateBountyTasks()">发布悬赏</button>
            <button id="bounty-abandon-btn" onclick="abandonBounty()" style="display:none">放弃任务</button>
            <button onclick="closeBountyModal()">关闭</button>
        </div>
    </div>
</div>
```

**（3）`showWorldMap()` 新增传参：**
```javascript
if (typeof activeBounty !== 'undefined' && activeBounty) {
    params.set('bountyLocation', activeBounty.locationName);
}
```

**（4）新增 `<script>` 引用：**
```html
<script src="module/bounty-service.js"></script>
```

---

### 5.5 修改 `world_map.html`

**（1）读取 bountyLocation 参数：**
```javascript
const bountyLocation = q.get('bountyLocation') || null;
```

**（2）渲染地点时叠加悬赏标记：**
在地点图标渲染逻辑中，若当前地点名 === bountyLocation，添加一个 `⚔` 角标。

**（3）`confirmTravel()` 新增字段：**
```javascript
window.parent.postMessage({
    type: 'worldmap-exit',
    mapLocation: currentLocation,
    companionNPC: selectedNPCs,
    randomEvent: randomEvent,
    battleEvent: battleEvent,
    bountyBattle: (bountyLocation && currentLocation === bountyLocation) ? 1 : 0
}, '*');
```

---

### 5.6 修改 `game-events.js`

**（1）`worldmap-exit` 消息处理 — 新增 bountyBattle 分支（在现有 randomEvent/battleEvent 处理之前）：**

```javascript
// 悬赏战斗：先发 LLM 请求叙述追踪过程，SIDENOTE 中构造战斗事件
if (event.data.bountyBattle === 1 && typeof activeBounty !== 'undefined' && activeBounty) {
    currentBattleType = 'bounty';  // 提前标记，battle-exit 时判断用

    // 构造 user 输入（同"下山游历"格式，key 触发 PROMPT_ACTION_BOUNTY）
    const year = Math.floor((currentWeek - 1) / 48) + 1;
    const remaining = (currentWeek - 1) % 48;
    const month = Math.floor(remaining / 4) + 1;
    const week = remaining % 4 + 1;
    const _actorName = gameData.playerName || '主角';
    const companionNames = /* 同 worldmap-exit 现有逻辑 */;

    const userMessage =
        `时间：第${year}年第${month}月第${week}周\n` +
        `季节：${seasonNameMap[seasonStatus] || '冬天'}\n` +
        `地点：${mapLocation || '天山派'}\n` +
        `随行NPC：${companionNames}\n` +
        `${_actorName}行动选择：悬赏缉拿`;

    // 关键：SIDENOTE 里是否输出"随机事件.战斗事件"JSON结构，由全局格式规范
    // （char_card_information/110格式规范_精简版_独立前端.txt）里的
    // <%_ if (battleEventforFormat == 1) { ... } _%> 条件决定，
    // 该模板读取的是 gameData.battleEvent，不是 PROMPT_ACTION_BOUNTY 行动指导本身能控制的。
    // 因此必须在调用 handleMessageOutput 前把 battleEvent 置 1（randomEvent 置 0 避免误触发选项事件），
    // handleMessageOutput 内部会调用 syncGameDataFromVariables() 把它同步进 gameData，
    // prompt-builder 构建格式规范模板时才能读到 gameData.battleEvent === 1。
    battleEvent = 1;
    randomEvent = 0;
    GameMode = 1;
    await handleMessageOutput(userMessage);
    return;  // 不走普通 worldmap-exit 的后续逻辑
}
```

**（2）`parseLLMResponse` — 覆盖 SIDENOTE 战斗数据 + 兜底（新增）：**

在 `parseLLMResponse` 检测到战斗事件后、调用 `displayBattleEvent` 前，插入：

```javascript
// 悬赏战斗：忽略 SIDENOTE 中的敌方数据，全部以 activeBounty 为准
// 战斗报酬固定写金钱，走 applyBattleReward 正常链路（显示"战斗胜利奖励：金钱+N"）
// 声望奖励在 battle-exit 分支里单独追加，不进 SIDENOTE
if (currentBattleType === 'bounty' && typeof activeBounty !== 'undefined' && activeBounty) {
    currentBattleEvent = {
        事件描述: `悬赏缉拿：${activeBounty.enemyName}`,
        事件类型: '战斗事件',
        敌方信息: {
            名称: activeBounty.enemyName,
            属性: { 武学: activeBounty.level, 生命力: '中', 攻击力: '中' },
            战斗报酬: { 类型: '金钱', 数值: activeBounty.goldReward }  // 固定金钱，无论 LLM 输出什么
        }
    };
}
```

**兜底：LLM 未输出战斗事件时**（SIDENOTE 格式错误或漏写），在 parseLLMResponse 执行完毕后检查：

```javascript
// 兜底：bounty 模式下 LLM 没给出战斗事件，主动构造并显示
if (currentBattleType === 'bounty' && !currentBattleEvent && typeof activeBounty !== 'undefined' && activeBounty) {
    console.warn('[bounty] LLM 未输出战斗事件，从 activeBounty 兜底构造');
    currentBattleEvent = {
        事件描述: `悬赏缉拿：${activeBounty.enemyName}`,
        事件类型: '战斗事件',
        敌方信息: {
            名称: activeBounty.enemyName,
            属性: { 武学: activeBounty.level, 生命力: '中', 攻击力: '中' },
            战斗报酬: { 类型: '金钱', 数值: activeBounty.goldReward }
        }
    };
    displayBattleEvent(currentBattleEvent);
}
```

**（3）"开始战斗"按钮 handler — currentBattleType 防覆盖判断（新增）：**

在原来设置 `currentBattleType = 'event'` 的地方，替换为：

```javascript
// 只防止被意外覆盖，不重新推导（worldmap-exit 已赋值 'bounty' 为准）
if (currentBattleType !== 'bounty') {
    currentBattleType = 'event';
}
```

**（4）`battle-exit` 消息处理 — 新增 `bounty` case：**

```javascript
} else if (currentBattleType === 'bounty') {
    // ① 同步道具（同 event 分支）
    if (event.data.remainingItems) { /* ...同现有逻辑... */ }

    const _bounty = activeBounty;
    const year = ...; const month = ...; const week = ...;
    const _actorName = gameData.playerName || '主角';
    const resultText = result === 'victory' ? `胜利，成功击败${_bounty?.enemyName}` :
                       result === 'quit'    ? `放弃战斗，目标逃脱` :
                                             `落败，目标成功逃脱`;

    // ② 胜利时应用奖励（在发请求前完成）
    // 金钱：通过 applyBattleReward 走标准链路（currentBattleEvent.敌方信息.战斗报酬 已在 parseLLMResponse 里写好）
    // 声望：悬赏专属奖励，在此处单独追加，不进 SIDENOTE
    let dropText = '';
    if (result === 'victory' && _bounty) {
        applyBattleReward(currentBattleEvent?.敌方信息?.战斗报酬);  // 金钱
        playerStats.声望 += _bounty.reputationReward;              // 声望（悬赏专属）
        checkAllValueRanges();
        updateStatsDisplay();
        const dropResult = generateBattleEventDrop(currentBattleEvent?.敌方信息);
        dropText = dropResult ? `\n获得掉落：${dropResult.itemName}` : '';
    }

    // ③ 清空任务，同步 gameData（在 handleMessageOutput 之前，确保快照干净）
    activeBounty = null;
    currentBattleType = null;
    currentBattleReward = null;
    syncGameDataFromVariables();

    // ④ 清空战斗事件 UI 状态（对齐现有 event 分支收尾动作，避免事件框/标志位残留）
    hideBattleEvent();  // 内部会将 currentBattleEvent 置 null、battleEvent 置 0

    // ⑤ 弹窗提示（异步，不阻塞）
    if (result === 'victory' && _bounty) {
        showModal(`悬赏达成！\n声望+${_bounty.reputationReward}\n赏金+${_bounty.goldReward}${dropText}`);
    }

    // ⑥ 发起第二轮 LLM 请求（handleMessageOutput 内部自动 saveFullSnapshot + saveAppState）
    await handleMessageOutput(
        `时间：第${year}年第${month}月第${week}周\n` +
        `季节：${seasonNameMap[seasonStatus] || '冬天'}\n` +
        `地点：${mapLocation || '天山派'}\n` +
        `${_actorName}缉拿结果：${resultText}`
    );
}
```

---

## 六、设计决策确认

### Q1：任务时效性
**决策：暂不加。** `acceptedWeek` 字段从 activeBounty 中移除，保持结构简洁。

### Q2：弹窗展示逻辑
**决策：有已接任务时，弹窗仅显示该任务卡（特殊高亮标记），底部只有"放弃任务"和"关闭"按钮，"发布悬赏"按钮不显示。放弃任务确认后，弹窗停留并切换回无任务状态（显示空列表+"发布悬赏"按钮），方便直接重新生成。**

弹窗的两种状态：
```
【无任务状态】              【有任务状态】
┌─────────────┐            ┌─────────────┐
│  悬赏公告    │            │  悬赏公告    │
│  （空）      │            │ ⚔ [当前任务] │  ← 高亮显示，标注"进行中"
│             │            │  目标/地点/  │
│             │            │  等级/奖励   │
├─────────────┤            ├─────────────┤
│ [发布悬赏]  │            │ [放弃任务]  │  ← 确认后切换回左侧状态
│ [关闭]      │            │ [关闭]      │
└─────────────┘            └─────────────┘
```

### Q3：地点名称
**决策：Prompt 中的地点列表与 `world_map.html` 的 key 完全一致。** 已核查代码确认：

| Prompt 中使用 | world_map.html key |
|---|---|
| 高昌 | `'高昌'` |
| 沙州 | `'沙州'`（注意：非"沙洲"也非"沙洲城"） |
| 龟兹 | `'龟兹'` |
| 伊州 | `'伊州'` |
| 哈密绿洲 | `'哈密绿洲'`（注意：非"哈密"） |
| 月牙泉 | `'月牙泉'` |

### Q4：悬赏战斗背景图
**决策：复用 `showBattleGame()` 中已有的 `window.__lastValidSceneUrl` 逻辑。**

由于悬赏缉拿的第一轮 LLM 输出会更新 `__lastValidSceneUrl`（正文中含目的地场景描写），`showBattleGame()` 被触发时 `GameMode === 1`，会自动取到最新的场景图，无需额外处理。

### Q5：重复接取限制
**决策：暂不限制。** 目标和地点每次由 LLM 随机生成，重复概率低；战斗结束后变量自动清空，机制上已有天然约束。

### Q6：放弃任务确认弹窗
**决策：加确认弹窗。** 点击"放弃任务"后弹出确认，确认后清空 `activeBounty`，弹窗切换回无任务状态而非关闭。

---

## 七、文件改动汇总

| 文件 | 操作 | 说明 |
|------|------|------|
| `module/bounty-service.js` | **新增** | 悬赏核心逻辑（生成/接取/放弃/UI） |
| `char_card_information/100行动选择-悬赏缉拿.txt` | **新增** | 悬赏行动指导提示词（供 LLM 叙述追踪剧情+构造战斗事件） |
| `module/prompt-data-actions.js` | 修改 | 注册 key "行动选择：悬赏缉拿" → `PROMPT_ACTION_BOUNTY` |
| `module/game-state.js` | 修改 | 新增 `activeBounty` 变量及双向同步 |
| `index.html` | 修改 | 议事厅加按钮、弹窗HTML、showWorldMap 传参、script引用 |
| `world_map.html` | 修改 | 读取 bountyLocation 参数、显示⚔标记、postMessage 加字段 |
| `game-events.js` | 修改 | worldmap-exit 新增 bounty 分支（发第一轮请求）；battle-exit 新增 bounty 分支（发第二轮请求） |

---

## 八、开发顺序建议

1. `game-state.js` — 加 `activeBounty` 变量（5分钟）
2. `bounty-service.js` — 核心逻辑（最复杂，约2-3小时）
3. `index.html` — 按钮 + 弹窗 HTML + showWorldMap 传参
4. `world_map.html` — 标记显示 + postMessage 字段
5. `game-events.js` — 两处消息处理
6. 联调测试

---

## 九、对 `index - SR.html`（SillyTavern 链路）的影响评估

**结论：本功能对 SR 链路安全，无论共享文件的 jsdelivr commit hash 是否/何时同步到最新，都不会被触发。**

### 9.1 引入方式差异
- `index - SR.html` 通过 jsdelivr **锁定 commit hash**（如 `@5b01b14`）引入以下共享模块：
  `game-config.js / item-list.js / skill-list.js / game-utils.js / game-state.js / game-ui.js / game-helpers.js / game-events.js / special-event.js / game-skills.js / button-sfx.js`。
  仓库改动不会自动生效，需要有人手动把 hash 改成包含新提交的版本才算"发版"。
- `world_map.html` 通过 GitHub Pages **直链**引入（`https://Ji-Haitang.github.io/char_card_1/world_map.html`），**没有 commit 锁定**，一旦推送到主分支就立即对 SR 生效。
- `prompt-builder.js` / `api-service.js` / `prompt-data-actions.js` / 新增的 `bounty-service.js`，以及 `index.html` 本身，**完全不被 `index - SR.html` 引用**，改动零风险。

### 9.2 为什么"即使 commit hash 未来同步"也依然安全
安全性不是来自"暂时没同步"，而是**触发链在 SR 侧物理不可达**：
- `bountyBattle` 标志位唯一来源是 `world_map.html` 的 `confirmTravel()`：`(bountyLocation && currentLocation === bountyLocation) ? 1 : 0`。
- `bountyLocation` 参数只能来自 URL query，而这个 query 是由"打开世界地图"的 `showWorldMap()` 函数拼出来的。
- `index - SR.html` 有**自己独立、不共享的 `showWorldMap()` 实现**（约 L3243），拼的参数里只有 `reputation/mapLocation/randomEvent/battleEvent/companionNPC/npcFavorability`，**没有也不可能有 `bountyLocation`**——因为 `bounty-service.js` 根本没被 SR 引入，没有任何代码路径能往 SR 的 `showWorldMap()` 里注入这个参数。
- `index - SR.html` **没有自己的 `worldmap-exit`/`battle-exit` 消息监听**，这两个事件完全由共享的 `game-events.js` 处理；但由于上一条，`bountyBattle` 在 SR 场景下永远是 `0`/`undefined`，`currentBattleType` 永远不会被赋值成 `'bounty'`，两个新分支即使代码同步过去也是死代码。

### 9.3 唯一的残留风险（非本次改动引入，仅供未来参考）
如果以后有人在 `index - SR.html` 里也照抄了悬赏入口（复制按钮/弹窗代码），但没有同步移植 `bounty-service.js`、`PROMPT_ACTION_BOUNTY` 等配套设施，会导致 SR 端出现"部分接线"的半成品 bug。这是假设性的未来变更风险，不是本次开发引入的问题，届时需要重新走一遍本节的核对流程。

### 9.4 实现注意事项（保证 9.2 的前提不被破坏）
- `world_map.html` 新增的 `bountyLocation` 相关代码，必须在参数缺省（`null`/`undefined`）时是完全的 no-op：图标标记判断用 `currentLocation === bountyLocation`（`bountyLocation` 为 `null` 时天然不相等，不加标记）；`bountyBattle` 计算式在 `bountyLocation` 为空时恒为 `0`。
- 不要把 `worldmap-exit`/`battle-exit` 里的 bounty 分支写成无条件执行，必须保持"先判断标志位再进入分支"的写法。

---

*文档版本：v0.1 初稿，待讨论确认后进入开发*
